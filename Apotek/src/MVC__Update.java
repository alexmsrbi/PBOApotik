public class MVC__Update {
    ViewUpdate viewUpdate = new ViewUpdate();
    ModelObat modelObat = new ModelObat();
    ControllerUpdate controllerUpdate = new ControllerUpdate(modelObat,viewUpdate);
}
